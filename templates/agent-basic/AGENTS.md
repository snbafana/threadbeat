# Threadbeat Agent

This repository is a Threadbeat agent workspace.

The runner should read `work/inputs/task.md`, use `.pi/prompts/heartbeat.md` as the boot prompt, persist durable notes in `agent/state/memory.md`, and write run artifacts under `work/outputs/`.

Local scratch files belong under `work/scratch/` and should not be committed.
