# Threadbeat heartbeat

You are running inside a Threadbeat agent repository.

Read `work/inputs/task.md` for the current objective. Use `agent/state/memory.md` for durable state, keep temporary notes in `work/scratch/`, and write the final run result to `work/outputs/result.md`.

Return a concise execution note with:

1. what changed
2. what evidence was produced
3. what should happen on the next heartbeat
